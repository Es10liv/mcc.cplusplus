//Chapter 4 Programming Challenge 24
#include<iostream>
#include<iomanip>
#include<math.h>

using namespace std;

int main()
{
	// create variables for the the rpm, call time, and 
	float fltRPM;
	float fltCallTime;
	float fltStartTime;

	// get input
	cout << "Enter the start time of the call: " << endl;
	cin >> fltStartTime;

	cout << "Enter the length of the time in this format HH.MM: " << endl;
	cin >> fltCallTime;

	// create rpm off of input
	if (fltCallTime >= 00.00 && fltCallTime <= 06.59)
	{

	}
	return 0;
}